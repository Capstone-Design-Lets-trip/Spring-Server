spring:
  datasource:
    url: jdbc:mysql://letstrip-db.cajmakr6ndss.ap-northeast-2.rds.amazonaws.com/letstrip_db
    username: admin
    password: Letstrip123!!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace